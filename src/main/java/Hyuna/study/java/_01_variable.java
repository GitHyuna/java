package Hyuna.study.java;

public class _01_variable {
    //1. 변수(varible) : 단 하나의 값을 저장할 수 잇는 메모리 공간
    int age1;    // age라는 이름의 변수를 선언

    //2. 변수의 초기화(initialization) : 다른 프로그램에 의해 저장된 알 수 없는 값(쓰레기 값)이 남아 있을 수 있기 때문에 초기화를 반드시 해야한다.
    //변수의 초기화란 변수를 사용하기 전에 처음 값을 저장하는 것
    int age2 = 25;   //  변수 age를 선언하고 25로 초기화 한다.

    //3. 변수선언와 초기화의 여러 방법
//    int a;
//    int b;
//    int x = 0;
//    int y = 0;

    //위의 코드를 아래와 같이 쓸 수도 있음

    int a, b;
    int x = 0, y = 0;
}
